The driver class represents an abstract driver. All of the drivers are children of this class.

It has 2 functions:
1. setup_driver_and_device() - Does whatever necessery for the driver to work.
3. exist()  - Does the corresponding device exist? Does not have to be called after attach.